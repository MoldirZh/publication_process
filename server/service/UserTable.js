import { db } from "../models/db";

export const QueryListOfUsers = (res, req) => {
    //select * from users
}

export const QueryUserById = (id) => {
    //select * from users where id=id
}

export const DeleteUserById = (id) => {
    //DELETE FROM users WHERE id=id
}
